
//This file should be called F_score.h
//This is autogenerated code. Edit by hand at your peril!

#include <stdint.h>
#include <stdbool.h>

//the dtimer_t type
typedef uint64_t dtimer_t;

//For each policy, we need an enum type for the state machine

enum score_policy_safe_states { 
	POLICY_STATE_score_safe_rabbit, 
	POLICY_STATE_score_safe_wolves 
};


//Inputs to the function score
typedef struct {
	int32_t rx;
	int32_t ry;
	int32_t w1x;
	int32_t w1y;
	int32_t w2x;
	int32_t w2y;
	int32_t exit1x;
	int32_t exit1y;
	int32_t exit2x;
	int32_t exit2y;
	int32_t exit3x;
	int32_t exit3y;
	
} inputs_score_t;

//Outputs from the function score
typedef struct {
	int32_t new_rx;
	int32_t new_ry;
	int32_t new_w1x;
	int32_t new_w1y;
	int32_t new_w2x;
	int32_t new_w2y;
	
} outputs_score_t;

//enforcer state and vars:
typedef struct {
	enum score_policy_safe_states _policy_safe_state;
	//internal vars
	dtimer_t turn_timer;
	
	
} enforcervars_score_t;

//This function is provided in "F_score.c"
//It sets up the variable structures to their initial values
void score_init_all_vars(enforcervars_score_t* me, inputs_score_t* inputs, outputs_score_t* outputs);

//This function is provided in "F_score.c"
//It will run the synthesised enforcer and call the controller function
void score_run_via_enforcer(enforcervars_score_t* me, inputs_score_t* inputs, outputs_score_t* outputs);

//This function is provided from the user
//It is the controller function
extern void score_run(inputs_score_t* inputs, outputs_score_t* outputs);

//enforcer functions


//This function is provided in "F_score.c"
//It will run the input enforcer for score's policy safe
void score_run_input_enforcer_safe(enforcervars_score_t* me, inputs_score_t* inputs);

//This function is provided in "F_score.c"
//It will run the input enforcer for score's policy safe
void score_run_output_enforcer_safe(enforcervars_score_t* me, inputs_score_t* inputs, outputs_score_t* outputs);

