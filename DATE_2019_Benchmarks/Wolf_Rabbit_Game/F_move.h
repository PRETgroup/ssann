
//This file should be called F_move.h
//This is autogenerated code. Edit by hand at your peril!

#include <stdint.h>
#include <stdbool.h>

//the dtimer_t type
typedef uint64_t dtimer_t;

//For each policy, we need an enum type for the state machine

enum move_policy_safe_states { 
	POLICY_STATE_move_safe_rabbit, 
	POLICY_STATE_move_safe_wolves 
};


//Inputs to the function move
typedef struct {
	int32_t rx;
	int32_t ry;
	int32_t w1x;
	int32_t w1y;
	int32_t w2x;
	int32_t w2y;
	int32_t exit1x;
	int32_t exit1y;
	int32_t exit2x;
	int32_t exit2y;
	int32_t exit3x;
	int32_t exit3y;
	
} inputs_move_t;

//Outputs from the function move
typedef struct {
	int32_t new_rx;
	int32_t new_ry;
	int32_t new_w1x;
	int32_t new_w1y;
	int32_t new_w2x;
	int32_t new_w2y;
	
} outputs_move_t;

//enforcer state and vars:
typedef struct {
	enum move_policy_safe_states _policy_safe_state;
	//internal vars
	dtimer_t turn_timer;
	int32_t boundry;
	
	
} enforcervars_move_t;

//This function is provided in "F_move.c"
//It sets up the variable structures to their initial values
void move_init_all_vars(enforcervars_move_t* me, inputs_move_t* inputs, outputs_move_t* outputs);

//This function is provided in "F_move.c"
//It will run the synthesised enforcer and call the controller function
void move_run_via_enforcer(enforcervars_move_t* me, inputs_move_t* inputs, outputs_move_t* outputs);

//This function is provided from the user
//It is the controller function
extern void move_run(inputs_move_t* inputs, outputs_move_t* outputs);

//enforcer functions


//This function is provided in "F_move.c"
//It will run the input enforcer for move's policy safe
void move_run_input_enforcer_safe(enforcervars_move_t* me, inputs_move_t* inputs);

//This function is provided in "F_move.c"
//It will run the input enforcer for move's policy safe
void move_run_output_enforcer_safe(enforcervars_move_t* me, inputs_move_t* inputs, outputs_move_t* outputs);

