
//This file should be called F_soc.c
//This is autogenerated code. Edit by hand at your peril!
#include "F_soc.h"

void soc_init_all_vars(enforcervars_soc_t* me, inputs_soc_t* inputs, outputs_soc_t* outputs) {
	//set any input vars with default values
	
	//set any output vars with default values
	outputs->charge = 0;
	outputs->watts = 0;
	

	
	me->_policy_safe_state = POLICY_STATE_soc_safe_output;
	//input policy internal vars
	
	me->max_capacity = (589*60);
	
	me->min_capacity = (155*60);
	
	
}

void soc_run_via_enforcer(enforcervars_soc_t* me, inputs_soc_t* inputs, outputs_soc_t* outputs) {
	//run the policies in reverse order for the inputs (last policies have highest priority)
	
	 soc_run_input_enforcer_safe(me, inputs);
	

	soc_run(inputs, outputs);

	//run policies in specified order for outputs
	soc_run_output_enforcer_safe(me, inputs,outputs);
	
}


//input policies

//INPUT POLICY safe BEGIN
//This will run the input enforcer for soc's policy safe
void soc_run_input_enforcer_safe(enforcervars_soc_t* me, inputs_soc_t* inputs) {
	switch(me->_policy_safe_state) {
		case POLICY_STATE_soc_safe_output:
			
			if(inputs->SoC < me->min_capacity) {
				//transition output -> violation on SoC < min_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->SoC < me->min_capacity) {
				//transition output -> violation on SoC < min_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->SoC > me->max_capacity) {
				//transition output -> violation on SoC > max_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->SoC > me->max_capacity) {
				//transition output -> violation on SoC > max_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			
			break;

		
	}
}

//INPUT POLICY safe END



//output policies

//OUTPUT POLICY safe BEGIN
//This will run the input enforcer for soc's policy safe
void soc_run_output_enforcer_safe(enforcervars_soc_t* me, inputs_soc_t* inputs, outputs_soc_t* outputs) {
	//advance timers
	
	
	//run enforcer
	switch(me->_policy_safe_state) {
		case POLICY_STATE_soc_safe_output:
			
			if(inputs->SoC < me->min_capacity) {
				//transition output -> violation on SoC < min_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->charge = 1;
				
			} 
			if(inputs->SoC < me->min_capacity) {
				//transition output -> violation on SoC < min_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->watts = me->min_capacity - inputs->SoC;
				
			} 
			if(inputs->SoC > me->max_capacity) {
				//transition output -> violation on SoC > max_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->charge = 0;
				
			} 
			if(inputs->SoC > me->max_capacity) {
				//transition output -> violation on SoC > max_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->watts = inputs->SoC - me->max_capacity;
				
			} 
			if(outputs->charge && inputs->SoC + outputs->watts > me->max_capacity) {
				//transition output -> violation on charge and SoC + watts > max_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->watts = ( me->max_capacity - inputs->SoC );
				
			} 
			if( !outputs->charge && inputs->SoC - outputs->watts < me->min_capacity) {
				//transition output -> violation on not (charge) and SoC - watts < min_capacity
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->watts = ( inputs->SoC - me->min_capacity );
				
			} 

			break;

		
	}

	//select transition to advance state
	switch(me->_policy_safe_state) {
		case POLICY_STATE_soc_safe_output:
			
			if(inputs->SoC >= me->min_capacity && inputs->SoC <= me->max_capacity && inputs->SoC + outputs->watts <= me->max_capacity) {
				//transition output -> output on SoC >= min_capacity and SoC <= max_capacity and SoC + watts <= max_capacity
				me->_policy_safe_state = POLICY_STATE_soc_safe_output;
				//set expressions
				
			} 
			if(inputs->SoC >= me->min_capacity && inputs->SoC <= me->max_capacity && inputs->SoC - outputs->watts >= me->min_capacity) {
				//transition output -> output on SoC >= min_capacity and SoC <= max_capacity and SoC - watts >= min_capacity
				me->_policy_safe_state = POLICY_STATE_soc_safe_output;
				//set expressions
				
			} 
			
			break;

		
	}
}

//OUTPUT POLICY safe END



