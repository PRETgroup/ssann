
//This file should be called F_soc.h
//This is autogenerated code. Edit by hand at your peril!

#include <stdint.h>
#include <stdbool.h>

//the dtimer_t type
typedef uint64_t dtimer_t;

//For each policy, we need an enum type for the state machine

enum soc_policy_safe_states { 
	POLICY_STATE_soc_safe_output 
};


//Inputs to the function soc
typedef struct {
	int32_t SoC;
	
} inputs_soc_t;

//Outputs from the function soc
typedef struct {
	bool charge;
	int32_t watts;
	
} outputs_soc_t;

//enforcer state and vars:
typedef struct {
	enum soc_policy_safe_states _policy_safe_state;
	//internal vars
	int32_t max_capacity;
	int32_t min_capacity;
	
	
} enforcervars_soc_t;

//This function is provided in "F_soc.c"
//It sets up the variable structures to their initial values
void soc_init_all_vars(enforcervars_soc_t* me, inputs_soc_t* inputs, outputs_soc_t* outputs);

//This function is provided in "F_soc.c"
//It will run the synthesised enforcer and call the controller function
void soc_run_via_enforcer(enforcervars_soc_t* me, inputs_soc_t* inputs, outputs_soc_t* outputs);

//This function is provided from the user
//It is the controller function
extern void soc_run(inputs_soc_t* inputs, outputs_soc_t* outputs);

//enforcer functions


//This function is provided in "F_soc.c"
//It will run the input enforcer for soc's policy safe
void soc_run_input_enforcer_safe(enforcervars_soc_t* me, inputs_soc_t* inputs);

//This function is provided in "F_soc.c"
//It will run the input enforcer for soc's policy safe
void soc_run_output_enforcer_safe(enforcervars_soc_t* me, inputs_soc_t* inputs, outputs_soc_t* outputs);

