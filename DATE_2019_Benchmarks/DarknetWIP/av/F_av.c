
//This file should be called F_av.c
//This is autogenerated code. Edit by hand at your peril!
#include "F_av.h"

void av_init_all_vars(enforcervars_av_t* me, inputs_av_t* inputs, outputs_av_t* outputs) {
	//set any input vars with default values
	
	//set any output vars with default values
	

	
	me->_policy_brake_state = POLICY_STATE_av_brake_cruise;
	//input policy internal vars
	
	me->t = 0;
	
	me->limit = 2;
	
	
}

void av_run_via_enforcer(enforcervars_av_t* me, inputs_av_t* inputs, outputs_av_t* outputs) {
	//run the policies in reverse order for the inputs (last policies have highest priority)
	
	 av_run_input_enforcer_brake(me, inputs);
	

	av_run(inputs, outputs);

	//run policies in specified order for outputs
	av_run_output_enforcer_brake(me, inputs,outputs);
	
}


//input policies

//INPUT POLICY brake BEGIN
//This will run the input enforcer for av's policy brake
void av_run_input_enforcer_brake(enforcervars_av_t* me, inputs_av_t* inputs) {
	switch(me->_policy_brake_state) {
		case POLICY_STATE_av_brake_cruise:
			
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 < 90) {
				//transition cruise -> violation on lobject0 < cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = inputs->lobject0;
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 < 90) {
				//transition cruise -> violation on lobject0 > cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = inputs->lobject0;
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 < 90) {
				//transition cruise -> violation on lobject1 < cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = inputs->lobject1;
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 < 90) {
				//transition cruise -> violation on lobject1 > cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = inputs->lobject1;
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 < 90) {
				//transition cruise -> violation on lobject2 < cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = inputs->lobject2;
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 < 90) {
				//transition cruise -> violation on lobject2 > cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = inputs->lobject2;
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 < 90) {
				//transition cruise -> violation on lobject3 < cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = inputs->lobject3;
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 < 90) {
				//transition cruise -> violation on lobject3 > cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = inputs->lobject3;
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 < 90) {
				//transition cruise -> violation on lobject4 < cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = inputs->lobject4;
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 < 90) {
				//transition cruise -> violation on lobject4 > cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = inputs->lobject4;
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = 3;
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = 3;
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = 3;
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = 3;
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = 3;
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = 3;
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = 3;
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = 3;
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = 3;
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = 3;
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject0 = 3;
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject0 = 3;
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject1 = 3;
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject1 = 3;
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject2 = 3;
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject2 = 3;
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject3 = 3;
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject3 = 3;
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject4 = 3;
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject4 = 3;
				
			} 
			if(inputs->cobject1 == 0 && inputs->cobject4 == 0) {
				//transition cruise -> violation on cobject1 = 0 and cobject4 = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->speed == 0) {
				//transition cruise -> violation on speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 0 && inputs->cobject4 == 0) {
				//transition cruise -> violation on cobject1 = 0 and cobject4 = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->speed == 0) {
				//transition cruise -> violation on speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->speed == 0) {
				//transition cruise -> violation on speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject3 == 2 && inputs->speed3 == 2 && inputs->speed == 1) {
				//transition cruise -> violation on cobject3 = 2 and speed3 = 2 and speed = 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 0 && inputs->speed > 0) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 0 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 0 && inputs->speed > 0) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 0 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->speed4 == 0 && inputs->speed > 0) {
				//transition cruise -> violation on cobject4 = 1 and speed4 = 0 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->speed4 == 0 && inputs->speed > 0) {
				//transition cruise -> violation on cobject4 = 3 and speed4 = 0 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 2 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 2 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 2 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 2 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->speed4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 1 and speed4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->speed4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 3 and speed4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 2 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 2 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 2 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 2 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 1 && inputs->speed4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 1 and speed4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 3 && inputs->speed4 == 0 && inputs->speed > 1) {
				//transition cruise -> violation on cobject4 = 3 and speed4 = 0 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 0) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 0) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 0) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 0) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 1 and speed > 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed >= inputs->speed1) {
				//transition cruise -> violation on cobject1 = 2 and speed >= speed1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1) {
				//transition cruise -> violation on cobject1 = 2 and speed > speed1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1) {
				//transition cruise -> violation on cobject1 = 2 and speed > speed1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed >= inputs->speed4) {
				//transition cruise -> violation on cobject4 = 2 and speed >= speed4
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4) {
				//transition cruise -> violation on cobject4 = 2 and speed > speed4
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4) {
				//transition cruise -> violation on cobject4 = 2 and speed > speed4
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->speed == 2) {
				//transition cruise -> violation on speed = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1) {
				//transition cruise -> violation on cobject1 = 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3) {
				//transition cruise -> violation on cobject1 = 3
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0) {
				//transition cruise -> violation on cobject1 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0) {
				//transition cruise -> violation on cobject1 = 3 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed == 0) {
				//transition cruise -> violation on cobject1 = 1 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed == 0) {
				//transition cruise -> violation on cobject1 = 3 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0) {
				//transition cruise -> violation on cobject1 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0) {
				//transition cruise -> violation on cobject1 = 3 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			
			break;

		case POLICY_STATE_av_brake_brake:
			
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 < 90) {
				//transition brake -> violation on lobject0 < cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = inputs->lobject0;
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 < 90) {
				//transition brake -> violation on lobject0 > cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = inputs->lobject0;
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 < 90) {
				//transition brake -> violation on lobject1 < cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = inputs->lobject1;
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 < 90) {
				//transition brake -> violation on lobject1 > cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = inputs->lobject1;
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 < 90) {
				//transition brake -> violation on lobject2 < cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = inputs->lobject2;
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 < 90) {
				//transition brake -> violation on lobject2 > cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = inputs->lobject2;
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 < 90) {
				//transition brake -> violation on lobject3 < cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = inputs->lobject3;
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 < 90) {
				//transition brake -> violation on lobject3 > cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = inputs->lobject3;
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 < 90) {
				//transition brake -> violation on lobject4 < cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = inputs->lobject4;
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 < 90) {
				//transition brake -> violation on lobject4 > cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = inputs->lobject4;
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = 3;
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject0 = 3;
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = 3;
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject1 = 3;
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = 3;
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject2 = 3;
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = 3;
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject3 = 3;
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = 3;
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->cobject4 = 3;
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject0 = 3;
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject0 = 3;
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject1 = 3;
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject1 = 3;
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject2 = 3;
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject2 = 3;
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject3 = 3;
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject3 = 3;
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject4 = 3;
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				inputs->lobject4 = 3;
				
			} 
			if(inputs->cobject1 == 1) {
				//transition brake -> violation on cobject1 = 1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3) {
				//transition brake -> violation on cobject1 = 3
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0) {
				//transition brake -> violation on cobject1 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0) {
				//transition brake -> violation on cobject1 = 3 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed == 0) {
				//transition brake -> violation on cobject1 = 1 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed == 0) {
				//transition brake -> violation on cobject1 = 3 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0) {
				//transition brake -> violation on cobject1 = 1 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0) {
				//transition brake -> violation on cobject1 = 3 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t < me->limit) {
				//transition brake -> violation on t < limit
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t < me->limit && inputs->speed > 0) {
				//transition brake -> violation on t < limit and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t < me->limit && inputs->speed > 0) {
				//transition brake -> violation on t < limit and speed > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 0) {
				//transition brake -> violation on t >= limit and cobject1 = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 2 && inputs->speed2 > 0) {
				//transition brake -> violation on t >= limit and cobject1 = 2 and speed2 > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 0) {
				//transition brake -> violation on t >= limit and cobject1 = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 2 && inputs->speed2 > 0) {
				//transition brake -> violation on t >= limit and cobject1 = 2 and speed2 > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 0) {
				//transition brake -> violation on t >= limit and cobject1 = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 2 && inputs->speed2 > 0) {
				//transition brake -> violation on t >= limit and cobject1 = 2 and speed2 > 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed >= inputs->speed1) {
				//transition brake -> violation on cobject1 = 2 and speed >= speed1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1) {
				//transition brake -> violation on cobject1 = 2 and speed > speed1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1) {
				//transition brake -> violation on cobject1 = 2 and speed > speed1
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed >= inputs->speed4) {
				//transition brake -> violation on cobject4 = 2 and speed >= speed4
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4) {
				//transition brake -> violation on cobject4 = 2 and speed > speed4
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4) {
				//transition brake -> violation on cobject4 = 2 and speed > speed4
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			
			break;

		
	}
}

//INPUT POLICY brake END



//output policies

//OUTPUT POLICY brake BEGIN
//This will run the input enforcer for av's policy brake
void av_run_output_enforcer_brake(enforcervars_av_t* me, inputs_av_t* inputs, outputs_av_t* outputs) {
	//advance timers
	
	me->t++;
	
	//run enforcer
	switch(me->_policy_brake_state) {
		case POLICY_STATE_av_brake_cruise:
			
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 < 90) {
				//transition cruise -> violation on lobject0 < cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 < 90) {
				//transition cruise -> violation on lobject0 > cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 < 90) {
				//transition cruise -> violation on lobject1 < cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 < 90) {
				//transition cruise -> violation on lobject1 > cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 < 90) {
				//transition cruise -> violation on lobject2 < cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 < 90) {
				//transition cruise -> violation on lobject2 > cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 < 90) {
				//transition cruise -> violation on lobject3 < cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 < 90) {
				//transition cruise -> violation on lobject3 > cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 < 90) {
				//transition cruise -> violation on lobject4 < cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 < 90) {
				//transition cruise -> violation on lobject4 > cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition cruise -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition cruise -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition cruise -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition cruise -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition cruise -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 0 && inputs->cobject4 == 0 && outputs->fast_brake) {
				//transition cruise -> violation on cobject1 = 0 and cobject4 = 0 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(outputs->fast_brake && inputs->speed == 0) {
				//transition cruise -> violation on fast_brake and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject1 == 0 && inputs->cobject4 == 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject1 = 0 and cobject4 = 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(outputs->slow_brake && inputs->speed == 0) {
				//transition cruise -> violation on slow_brake and speed = 0
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->speed == 0 && !outputs->accelerate) {
				//transition cruise -> violation on speed = 0 and not (accelerate)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 1;
				
			} 
			if(inputs->cobject3 == 2 && inputs->speed3 == 2 && inputs->speed == 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject3 = 2 and speed3 = 2 and speed = 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 0 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 0 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 0 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 0 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 2 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 2 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 2 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 2 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->speed4 == 0 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 1 and speed4 = 0 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 3 && inputs->speed4 == 0 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 3 and speed4 = 0 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 0 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 0 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 0 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 0 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 2 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 2 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 2 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 2 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->speed4 == 0 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 1 and speed4 = 0 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 3 && inputs->speed4 == 0 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 3 and speed4 = 0 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 0 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 0 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 0 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 0 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 1 && inputs->dir4 == 2 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 1 and dir4 = 2 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 3 && inputs->dir4 == 2 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 3 and dir4 = 2 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 1 && inputs->speed4 == 0 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 1 and speed4 = 0 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 3 && inputs->speed4 == 0 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 3 and speed4 = 0 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 1 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 1 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 1 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 1 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 1 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 1 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 1 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 1 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 0 && outputs->accelerate) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 1 and speed > 0 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 1 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 1 and speed > 1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 1 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 1 && inputs->speed > 1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 1 and speed > 1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed >= inputs->speed1 && outputs->accelerate) {
				//transition cruise -> violation on cobject1 = 2 and speed >= speed1 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1 && outputs->fast_brake) {
				//transition cruise -> violation on cobject1 = 2 and speed > speed1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject1 = 2 and speed > speed1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed >= inputs->speed4 && outputs->accelerate) {
				//transition cruise -> violation on cobject4 = 2 and speed >= speed4 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4 && outputs->fast_brake) {
				//transition cruise -> violation on cobject4 = 2 and speed > speed4 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject4 = 2 and speed > speed4 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->speed == 2 && outputs->accelerate) {
				//transition cruise -> violation on speed = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 1 && outputs->accelerate) {
				//transition cruise -> violation on cobject1 = 1 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 3 && outputs->accelerate) {
				//transition cruise -> violation on cobject1 = 3 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject1 = 1 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject1 = 3 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject1 = 1 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject1 = 3 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition cruise -> violation on cobject1 = 1 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition cruise -> violation on cobject1 = 3 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && outputs->accelerate) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && outputs->accelerate) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition cruise -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition cruise -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && outputs->accelerate) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && outputs->accelerate) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition cruise -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition cruise -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 

			break;

		case POLICY_STATE_av_brake_brake:
			
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 < 90) {
				//transition brake -> violation on lobject0 < cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 < 90) {
				//transition brake -> violation on lobject0 > cobject0 and conf0 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 < 90) {
				//transition brake -> violation on lobject1 < cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 < 90) {
				//transition brake -> violation on lobject1 > cobject1 and conf1 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 < 90) {
				//transition brake -> violation on lobject2 < cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 < 90) {
				//transition brake -> violation on lobject2 > cobject2 and conf2 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 < 90) {
				//transition brake -> violation on lobject3 < cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 < 90) {
				//transition brake -> violation on lobject3 > cobject3 and conf3 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 < 90) {
				//transition brake -> violation on lobject4 < cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 < 90) {
				//transition brake -> violation on lobject4 > cobject4 and conf4 < 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 < inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 < cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject0 > inputs->cobject0 && inputs->conf0 > 90) {
				//transition brake -> violation on lobject0 > cobject0 and conf0 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 < inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 < cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject1 > inputs->cobject1 && inputs->conf1 > 90) {
				//transition brake -> violation on lobject1 > cobject1 and conf1 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 < inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 < cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject2 > inputs->cobject2 && inputs->conf2 > 90) {
				//transition brake -> violation on lobject2 > cobject2 and conf2 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 < inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 < cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject3 > inputs->cobject3 && inputs->conf3 > 90) {
				//transition brake -> violation on lobject3 > cobject3 and conf3 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 < inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 < cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->lobject4 > inputs->cobject4 && inputs->conf4 > 90) {
				//transition brake -> violation on lobject4 > cobject4 and conf4 > 90
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				
			} 
			if(inputs->cobject1 == 1 && outputs->accelerate) {
				//transition brake -> violation on cobject1 = 1 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 3 && outputs->accelerate) {
				//transition brake -> violation on cobject1 = 3 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on cobject1 = 1 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on cobject1 = 3 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition brake -> violation on cobject1 = 1 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition brake -> violation on cobject1 = 3 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject1 == 1 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on cobject1 = 1 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject1 == 3 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on cobject1 = 3 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && outputs->accelerate) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && outputs->accelerate) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on cobject0 = 1 and dir0 = 1 and speed0 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on cobject0 = 3 and dir0 = 1 and speed0 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && outputs->accelerate) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && outputs->accelerate) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed == 0 && !outputs->slow_brake) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed = 0 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on cobject2 = 1 and dir2 = 3 and speed2 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 == 2 && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on cobject2 = 3 and dir2 = 3 and speed2 = 2 and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(me->t < me->limit && outputs->accelerate) {
				//transition brake -> violation on t < limit and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(me->t < me->limit && inputs->speed > 0 && outputs->slow_brake) {
				//transition brake -> violation on t < limit and speed > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(me->t < me->limit && inputs->speed > 0 && !outputs->fast_brake) {
				//transition brake -> violation on t < limit and speed > 0 and not (fast_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 1;
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 0 && !outputs->accelerate) {
				//transition brake -> violation on t >= limit and cobject1 = 0 and not (accelerate)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 1;
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 2 && inputs->speed2 > 0 && !outputs->accelerate) {
				//transition brake -> violation on t >= limit and cobject1 = 2 and speed2 > 0 and not (accelerate)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 1;
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 0 && outputs->slow_brake) {
				//transition brake -> violation on t >= limit and cobject1 = 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 2 && inputs->speed2 > 0 && outputs->slow_brake) {
				//transition brake -> violation on t >= limit and cobject1 = 2 and speed2 > 0 and slow_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 0;
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 0 && outputs->fast_brake) {
				//transition brake -> violation on t >= limit and cobject1 = 0 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(me->t >= me->limit && inputs->cobject1 == 2 && inputs->speed2 > 0 && outputs->fast_brake) {
				//transition brake -> violation on t >= limit and cobject1 = 2 and speed2 > 0 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed >= inputs->speed1 && outputs->accelerate) {
				//transition brake -> violation on cobject1 = 2 and speed >= speed1 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1 && outputs->fast_brake) {
				//transition brake -> violation on cobject1 = 2 and speed > speed1 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject1 == 2 && inputs->speed > inputs->speed1 && !outputs->slow_brake) {
				//transition brake -> violation on cobject1 = 2 and speed > speed1 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed >= inputs->speed4 && outputs->accelerate) {
				//transition brake -> violation on cobject4 = 2 and speed >= speed4 and accelerate
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->accelerate = 0;
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4 && outputs->fast_brake) {
				//transition brake -> violation on cobject4 = 2 and speed > speed4 and fast_brake
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->fast_brake = 0;
				
			} 
			if(inputs->cobject4 == 2 && inputs->speed > inputs->speed4 && !outputs->slow_brake) {
				//transition brake -> violation on cobject4 = 2 and speed > speed4 and not (slow_brake)
				//select a transition to solve the problem
				
				//Recovery instructions manually provided.
				outputs->slow_brake = 1;
				
			} 

			break;

		
	}

	//select transition to advance state
	switch(me->_policy_brake_state) {
		case POLICY_STATE_av_brake_cruise:
			
			if( !outputs->fast_brake && !outputs->slow_brake && inputs->cobject4 == 1 && inputs->speed4 == 0 && !outputs->slow_brake && inputs->cobject4 == 3 && inputs->speed4 == 0 && !outputs->slow_brake && inputs->cobject4 == 1 && inputs->dir4 == 2 && !outputs->slow_brake && inputs->cobject4 == 3 && inputs->dir4 == 2 && !outputs->slow_brake && inputs->cobject4 == 1 && inputs->dir4 == 0 && !outputs->slow_brake && inputs->cobject4 == 3 && inputs->dir4 == 0 && !outputs->slow_brake && inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 > 0 && !outputs->slow_brake && inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 > 0 && !outputs->slow_brake && inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 > 0 && !outputs->slow_brake && inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 > 0) {
				//transition cruise -> cruise on not (fast_brake) and not (slow_brake and cobject4 = 1 and speed4 = 0) and not (slow_brake and cobject4 = 3 and speed4 = 0) and not (slow_brake and cobject4 = 1 and dir4 = 2) and not (slow_brake and cobject4 = 3 and dir4 = 2) and not (slow_brake and cobject4 = 1 and dir4 = 0) and not (slow_brake and cobject4 = 3 and dir4 = 0) and not (slow_brake and cobject0 = 1 and dir0 = 1 and speed0 > 0) and not (slow_brake and cobject0 = 3 and dir0 = 1 and speed0 > 0) and not (slow_brake and cobject2 = 1 and dir2 = 3 and speed2 > 0) and not (slow_brake and cobject2 = 3 and dir2 = 3 and speed2 > 0)
				me->_policy_brake_state = POLICY_STATE_av_brake_cruise;
				//set expressions
				
			} 
			if(outputs->fast_brake) {
				//transition cruise -> brake on fast_brake
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject4 == 1 && inputs->speed4 == 0) {
				//transition cruise -> brake on slow_brake and cobject4 = 1 and speed4 = 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject4 == 3 && inputs->speed4 == 0) {
				//transition cruise -> brake on slow_brake and cobject4 = 3 and speed4 = 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject4 == 1 && inputs->dir4 == 2) {
				//transition cruise -> brake on slow_brake and cobject4 = 1 and dir4 = 2
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject4 == 3 && inputs->dir4 == 2) {
				//transition cruise -> brake on slow_brake and cobject4 = 3 and dir4 = 2
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject4 == 1 && inputs->dir4 == 0) {
				//transition cruise -> brake on slow_brake and cobject4 = 1 and dir4 = 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject4 == 3 && inputs->dir4 == 0) {
				//transition cruise -> brake on slow_brake and cobject4 = 3 and dir4 = 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject0 == 1 && inputs->dir0 == 1 && inputs->speed0 > 0) {
				//transition cruise -> brake on slow_brake and cobject0 = 1 and dir0 = 1 and speed0 > 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject0 == 3 && inputs->dir0 == 1 && inputs->speed0 > 0) {
				//transition cruise -> brake on slow_brake and cobject0 = 3 and dir0 = 1 and speed0 > 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject2 == 1 && inputs->dir2 == 3 && inputs->speed2 > 0) {
				//transition cruise -> brake on slow_brake and cobject2 = 1 and dir2 = 3 and speed2 > 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && inputs->cobject2 == 3 && inputs->dir2 == 3 && inputs->speed2 > 0) {
				//transition cruise -> brake on slow_brake and cobject2 = 3 and dir2 = 3 and speed2 > 0
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			
			break;

		case POLICY_STATE_av_brake_brake:
			
			if(me->t >= me->limit && outputs->accelerate) {
				//transition brake -> cruise on t >= limit and accelerate
				me->_policy_brake_state = POLICY_STATE_av_brake_cruise;
				//set expressions
				
			} 
			if(me->t < me->limit && !outputs->accelerate) {
				//transition brake -> brake on t < limit and not (accelerate)
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->fast_brake && !outputs->accelerate) {
				//transition brake -> brake on fast_brake and not (accelerate)
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			if(outputs->slow_brake && !outputs->accelerate) {
				//transition brake -> brake on slow_brake and not (accelerate)
				me->_policy_brake_state = POLICY_STATE_av_brake_brake;
				//set expressions
				
				me->t = 0;
			} 
			
			break;

		
	}
}

//OUTPUT POLICY brake END



